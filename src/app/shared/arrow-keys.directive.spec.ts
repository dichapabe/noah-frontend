import { ArrowKeysDirective } from './arrow-keys.directive';

describe('ArrowKeysDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowKeysDirective();
    expect(directive).toBeTruthy();
  });
});
